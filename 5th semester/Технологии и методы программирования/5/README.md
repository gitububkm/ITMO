# 🧩 Исторические шифры: «Решётка Кардано» и «Скитала»

Программа реализует **два классических алгоритма шифрования** —  
**Решётка (Кардано)** и **Шифр перестановки Скитала**,  
с графическим интерфейсом для демонстрации и учебной защиты лабораторной работы.

---

## 🚀 Возможности

- 🔐 **Два режима работы:**
  - «Решётка (Кардано)» — перестановочный шифр с 4 поворотами квадратной решётки.  
  - «Скитала (перестановка)» — древнегреческий шифр с фиксированным количеством рядов.

- 🧠 **Автоматическое определение**: программа сама анализирует вставленный текст и предлагает —
  «Расшифровать» или «Зашифровать повторно».

- 📂 **Работа с файлами**: открытие, сохранение, импорт/экспорт ключей решётки (JSON).

- ✍️ **Поддержка любого текста**:
  - Русский, английский, смешанный, цифры, символы, эмодзи.
  - Сохраняются все знаки препинания и регистры.
  - Любая длина текста (разбивается на блоки автоматически).

- 🧩 **Паддинг (заполнитель)** — символ `¤` по умолчанию (можно изменить).

- 🧰 **Интерфейс:**
  - Меню «Правка» + контекстное меню (ПКМ).
  - Горячие клавиши работают **в любой раскладке**:
    - `Ctrl+C/V/X/A`, `Ctrl+С/В/Х/А`, `Ctrl/Shift+Insert`, `Shift+Delete`.
  - Автоматический перенос строк, масштабируемое окно, Undo/Redo.

---

## 🧮 Описание алгоритмов

### 1️⃣ Шифр Скитала
**Идея:** текст записывается по строкам на воображаемом цилиндре, затем считывается по столбцам.  
**Ключ:** количество рядов (например, 3).


**Особенности:**
- Перестановочный шифр — сохраняет частоты символов.
- Очень прост, но иллюстрирует принцип «геометрического» кодирования.

---

### 2️⃣ Решётка Кардано
**Идея:** текст записывается в квадрат через отверстия в решётке,  
которая 4 раза поворачивается на 90°. Каждый символ попадает в уникальную клетку.

**Ключ:** квадрат `n×n` и координаты отверстий (для 4×4 встроен корректный ключ).


**Особенности:**
- Каждая клетка используется ровно один раз.
- Перестановка символов по трафарету.
- Хорошая демонстрация принципа «физического ключа» в криптографии.

---

## 📘 Интерфейс

### Главное окно
- Кнопки для выбора алгоритма.
- Подсказки и описание методов.

### Окно шифрования
- Поле ввода и поле результата.
- Кнопки:  
  🔸 «Автоанализ»  
  🔸 «Зашифровать»  
  🔸 «Расшифровать»  
  🔸 «Открыть файл» / «Сохранить результат»  
  🔸 (для решётки) «Импорт» и «Экспорт ключа JSON»
- В верхней панели можно изменить:
  - `Паддинг` (символ-заполнитель)
  - `Ряды` (для Скиталы)

---

## 🧰 Технические детали

| Компонент | Используется |
|------------|--------------|
| Язык | Python 3.9+ |
| GUI | `tkinter` (встроен в Python) |
| Сборка exe | `PyInstaller` |
| Файлы | `main.py`, `README.md`, опционально `app.ico` |

---

## ⚙️ Сборка `.exe` (Windows, без консоли)

```powershell
python -m pip install pyinstaller
pyinstaller --noconsole --onefile --name "Шифровальщик" main.py
