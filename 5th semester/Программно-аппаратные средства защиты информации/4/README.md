# Full-Stack Приложение для Регистрации Пользователей

Это полноценное MVP веб-приложение для регистрации и аутентификации пользователей, построенное на современном технологическом стеке и упакованное в Docker для легкого развертывания.

## 🚀 Технологический стек

-   **Frontend**: React 18, Vite, Axios, CSS3 (с дизайном в стиле Windows 7)
-   **Backend**: Python 3.12, FastAPI, SQLAlchemy (asyncio), Pydantic
-   **База данных**: PostgreSQL 16
-   **Контейнеризация**: Docker, Docker Compose
-   **Безопасность**: Хэширование паролей с помощью Argon2id, валидация данных, JWT-токены
-   **Тестирование**: Pytest, Httpx для асинхронных тестов
-   **Миграции БД**: Alembic

## ⚡️ Функциональность

-   Регистрация нового пользователя с надежной валидацией логина и пароля.
-   Аутентификация существующего пользователя и выдача JWT-токена.
-   Безопасное хранение паролей в базе данных с использованием криптостойкого алгоритма Argon2id.
-   Полная контейнеризация для разработки и продакшена.
-   Автоматически генерируемая OpenAPI документация (`/docs`).
-   Аутентичный пользовательский интерфейс в стиле Windows 7.

## 🛠️ Запуск проекта

### Требования
-   Docker
-   Docker Compose

### Инструкции по запуску

1.  **Клонируйте репозиторий:**
    ```bash
    git clone <repository-url>
    cd <project-folder>
    ```

2.  **Создайте и настройте файл окружения:**
    Скопируйте пример файла `.env.example` в `.env`. В Unix-системах (Linux, macOS, WSL) используйте `cp`, в Windows CMD `copy`.
    ```bash
    # Linux/macOS/WSL
    cp .env.example .env
    
    # Windows CMD
    copy .env.example .env
    ```
    При необходимости измените значения в `.env`. `SECRET_KEY` **обязательно** должен быть изменен на случайное, длинное и криптостойкое значение для продакшен-среды.

3.  **Соберите и запустите контейнеры:**
    Эта команда соберет production-ready образы и запустит все сервисы в фоновом режиме.
    ```bash
    docker compose up --build -d
    ```

4.  **Проверка статуса:**
    ```bash
    docker compose ps
    ```
    Вы должны увидеть три запущенных сервиса: `broiler_frontend`, `broiler_backend`, `broiler_db`.

### Запуск автотестов

Для проверки корректности работы backend-части предусмотрен набор автоматических тестов. Для их запуска выполните команду:

```bash
docker compose run --rm backend python -m pytest
```
Эта команда запустит временный контейнер, который выполнит все тесты с использованием отдельной тестовой базы данных и будет удален после завершения.

### Доступ к приложению
-   **Веб-интерфейс**: [http://localhost](http://localhost)
-   **API документация (Swagger UI)**: [http://localhost:8000/docs](http://localhost:8000/docs)
-   **Подключение к БД (для разработки)**: `host: localhost`, `port: 5432`

## 🔐 Безопасность: что сделано и почему

1.  **Хэширование паролей с Argon2id:**
    *   **Что:** Пароли пользователей никогда не хранятся в открытом виде. Вместо этого мы храним их хэш, полученный с помощью криптостойкого алгоритма Argon2id.
    *   **Почему:** Argon2id является победителем конкурса Password Hashing Competition и рекомендуется как один из самых надежных алгоритмов. Он устойчив к атакам перебора по словарю, радужным таблицам и, что важно, к атакам на GPU благодаря настраиваемым параметрам "памяти" и "времени".

2.  **Защита от дублирования логинов:**
    *   **Что:** На уровне базы данных для поля `login` установлен уникальный индекс (`UNIQUE`).
    *   **Почему:** Это гарантирует целостность данных и предотвращает создание двух пользователей с одинаковым логином на самом надежном уровне — уровне СУБД.

3.  **Строгая валидация входных данных:**
    *   **Что:** Все данные, приходящие от клиента (и на фронтенде, и на бэкенде), проходят строгую валидацию. Проверяется длина, допустимые символы для логина и сложность пароля.
    *   **Почему:** Это основной механизм защиты от множества атак, включая инъекции (SQL, NoSQL), XSS и переполнение буфера. Мы доверяем только тем данным, которые соответствуют заранее определенным правилам.

4.  **Безопасная конфигурация через переменные окружения:**
    *   **Что:** Все чувствительные данные (ключи, пароли от БД, параметры) вынесены в `.env` файл и не хранятся в коде.
    *   **Почему:** Это соответствует принципу "код отдельно, конфигурация отдельно". Это предотвращает случайную утечку секретов в систему контроля версий (Git).

5.  **Минимизированные Docker-образы для продакшена:**
    *   **Что:** Используются многостадийные сборки Docker, чтобы в финальных образах не было инструментов для разработки (компиляторов, исходного кода Node.js и т.д.).
    *   **Почему:** Меньший размер образа означает меньшую поверхность атаки. Чем меньше утилит в контейнере, тем меньше у злоумышленника возможностей в случае компрометации.

6.  **Отсутствие логгирования чувствительных данных:**
    *   **Что:** В логах фиксируются события (успешная регистрация, ошибка аутентификации), но никогда не записывается сырой пароль пользователя.
    *   **Почему:** Логи могут быть скомпрометированы. Исключение паролей из логов — обязательное правило "безопасного логгирования".

## 📁 Структура проекта

```
├── backend/                # FastAPI сервер
│   ├── app/                # Основной код приложения
│   │   ├── api/            # API эндпоинты и зависимости
│   │   ├── core/           # Конфигурация и безопасность
│   │   ├── db/             # Модели и сессии БД
│   │   ├── repositories/   # Слой доступа к данным
│   │   ├── schemas/        # Pydantic схемы
│   │   └── services/       # Бизнес-логика
│   ├── alembic/            # Миграции БД
│   ├── tests/              # Автотесты
│   └── Dockerfile          # Production-ready Dockerfile
│
├── frontend/               # React приложение
│   ├── public/             # Статические ассеты
│   ├── src/                # Исходный код
│   ├── Dockerfile          # Production-ready Dockerfile с Nginx
│   └── nginx.conf          # Конфигурация Nginx
│
├── docker-compose.yml      # Оркестрация контейнеров
├── .env.example            # Пример переменных окружения
└── README.md               # Эта документация
```

## API Эндпоинты (Примеры с curl)

### Регистрация

```bash
curl -X 'POST' \
  'http://localhost:8000/api/v1/auth/register' \
  -H 'accept: application/json' \
  -H 'Content-Type: application/json' \
  -d '{
  "login": "testuser",
  "password": "SecurePassword1!"
}'
```

### Аутентификация

```bash
curl -X 'POST' \
  'http://localhost:8000/api/v1/auth/login' \
  -H 'accept: application/json' \
  -H 'Content-Type: application/json' \
  -d '{
  "login": "testuser",
  "password": "SecurePassword1!"
}'
```
